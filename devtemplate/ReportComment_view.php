<?php
// This script and data application were generated by AppGini 5.72
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir=dirname(__FILE__);
	include("$currDir/defaultLang.php");
	include("$currDir/language.php");
	include("$currDir/lib.php");
	@include("$currDir/hooks/ReportComment.php");
	include("$currDir/ReportComment_dml.php");

	// mm: can the current member access this page?
	$perm=getTablePermissions('ReportComment');
	if(!$perm[0]){
		echo error_message($Translation['tableAccessDenied'], false);
		echo '<script>setTimeout("window.location=\'index.php?signOut=1\'", 2000);</script>';
		exit;
	}

	$x = new DataList;
	$x->TableName = "ReportComment";

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = array(   
		"`ReportComment`.`RunningID`" => "RunningID",
		"IF(    CHAR_LENGTH(`IMSReport1`.`Title`) || CHAR_LENGTH(if(`IMSReport1`.`filed`,date_format(`IMSReport1`.`filed`,'%m/%d/%Y %h:%i %p'),'')), CONCAT_WS('',   `IMSReport1`.`Title`, ' @', if(`IMSReport1`.`filed`,date_format(`IMSReport1`.`filed`,'%m/%d/%Y %h:%i %p'),'')), '') /* Commenting on */" => "PostID",
		"`ReportComment`.`TextPost`" => "TextPost",
		"if(`ReportComment`.`filed`,date_format(`ReportComment`.`filed`,'%m/%d/%Y %h:%i %p'),'')" => "filed",
		"if(`ReportComment`.`last_modified`,date_format(`ReportComment`.`last_modified`,'%m/%d/%Y %h:%i %p'),'')" => "last_modified"
	);
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = array(   
		1 => '`ReportComment`.`RunningID`',
		2 => 2,
		3 => 3,
		4 => '`ReportComment`.`filed`',
		5 => '`ReportComment`.`last_modified`'
	);

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = array(   
		"`ReportComment`.`RunningID`" => "RunningID",
		"IF(    CHAR_LENGTH(`IMSReport1`.`Title`) || CHAR_LENGTH(if(`IMSReport1`.`filed`,date_format(`IMSReport1`.`filed`,'%m/%d/%Y %h:%i %p'),'')), CONCAT_WS('',   `IMSReport1`.`Title`, ' @', if(`IMSReport1`.`filed`,date_format(`IMSReport1`.`filed`,'%m/%d/%Y %h:%i %p'),'')), '') /* Commenting on */" => "PostID",
		"`ReportComment`.`TextPost`" => "TextPost",
		"if(`ReportComment`.`filed`,date_format(`ReportComment`.`filed`,'%m/%d/%Y %h:%i %p'),'')" => "filed",
		"if(`ReportComment`.`last_modified`,date_format(`ReportComment`.`last_modified`,'%m/%d/%Y %h:%i %p'),'')" => "last_modified"
	);
	// Fields that can be filtered
	$x->QueryFieldsFilters = array(   
		"`ReportComment`.`RunningID`" => "Comment ID",
		"IF(    CHAR_LENGTH(`IMSReport1`.`Title`) || CHAR_LENGTH(if(`IMSReport1`.`filed`,date_format(`IMSReport1`.`filed`,'%m/%d/%Y %h:%i %p'),'')), CONCAT_WS('',   `IMSReport1`.`Title`, ' @', if(`IMSReport1`.`filed`,date_format(`IMSReport1`.`filed`,'%m/%d/%Y %h:%i %p'),'')), '') /* Commenting on */" => "Commenting on",
		"`ReportComment`.`TextPost`" => "Comment",
		"`ReportComment`.`filed`" => "Write on",
		"`ReportComment`.`last_modified`" => "Last modified"
	);

	// Fields that can be quick searched
	$x->QueryFieldsQS = array(   
		"`ReportComment`.`RunningID`" => "RunningID",
		"IF(    CHAR_LENGTH(`IMSReport1`.`Title`) || CHAR_LENGTH(if(`IMSReport1`.`filed`,date_format(`IMSReport1`.`filed`,'%m/%d/%Y %h:%i %p'),'')), CONCAT_WS('',   `IMSReport1`.`Title`, ' @', if(`IMSReport1`.`filed`,date_format(`IMSReport1`.`filed`,'%m/%d/%Y %h:%i %p'),'')), '') /* Commenting on */" => "PostID",
		"`ReportComment`.`TextPost`" => "TextPost",
		"if(`ReportComment`.`filed`,date_format(`ReportComment`.`filed`,'%m/%d/%Y %h:%i %p'),'')" => "filed",
		"if(`ReportComment`.`last_modified`,date_format(`ReportComment`.`last_modified`,'%m/%d/%Y %h:%i %p'),'')" => "last_modified"
	);

	// Lookup fields that can be used as filterers
	$x->filterers = array(  'PostID' => 'Commenting on');

	$x->QueryFrom = "`ReportComment` LEFT JOIN `IMSReport` as IMSReport1 ON `IMSReport1`.`Postid`=`ReportComment`.`PostID` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm[2]==0 ? 1 : 0);
	$x->AllowDelete = $perm[4];
	$x->AllowMassDelete = true;
	$x->AllowInsert = $perm[1];
	$x->AllowUpdate = $perm[3];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = 1;
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation["quick search"];
	$x->ScriptFileName = "ReportComment_view.php";
	$x->RedirectAfterInsert = "ReportComment_view.php?SelectedID=#ID#";
	$x->TableTitle = "Report Comment";
	$x->TableIcon = "resources/table_icons/application_form_magnify.png";
	$x->PrimaryKey = "`ReportComment`.`RunningID`";
	$x->DefaultSortField = '2';
	$x->DefaultSortDirection = 'desc';

	$x->ColWidth   = array(  70, 150, 150, 150);
	$x->ColCaption = array("Commenting on", "Comment", "Write on", "Last modified");
	$x->ColFieldName = array('PostID', 'TextPost', 'filed', 'last_modified');
	$x->ColNumber  = array(2, 3, 4, 5);

	// template paths below are based on the app main directory
	$x->Template = 'templates/ReportComment_templateTV.html';
	$x->SelectedTemplate = 'templates/ReportComment_templateTVS.html';
	$x->TemplateDV = 'templates/ReportComment_templateDV.html';
	$x->TemplateDVP = 'templates/ReportComment_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HighlightColor = '#FFF0C2';

	// mm: build the query based on current member's permissions
	$DisplayRecords = $_REQUEST['DisplayRecords'];
	if(!in_array($DisplayRecords, array('user', 'group'))){ $DisplayRecords = 'all'; }
	if($perm[2]==1 || ($perm[2]>1 && $DisplayRecords=='user' && !$_REQUEST['NoFilter_x'])){ // view owner only
		$x->QueryFrom.=', membership_userrecords';
		$x->QueryWhere="where `ReportComment`.`RunningID`=membership_userrecords.pkValue and membership_userrecords.tableName='ReportComment' and lcase(membership_userrecords.memberID)='".getLoggedMemberID()."'";
	}elseif($perm[2]==2 || ($perm[2]>2 && $DisplayRecords=='group' && !$_REQUEST['NoFilter_x'])){ // view group only
		$x->QueryFrom.=', membership_userrecords';
		$x->QueryWhere="where `ReportComment`.`RunningID`=membership_userrecords.pkValue and membership_userrecords.tableName='ReportComment' and membership_userrecords.groupID='".getLoggedGroupID()."'";
	}elseif($perm[2]==3){ // view all
		// no further action
	}elseif($perm[2]==0){ // view none
		$x->QueryFields = array("Not enough permissions" => "NEP");
		$x->QueryFrom = '`ReportComment`';
		$x->QueryWhere = '';
		$x->DefaultSortField = '';
	}
	// hook: ReportComment_init
	$render=TRUE;
	if(function_exists('ReportComment_init')){
		$args=array();
		$render=ReportComment_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: ReportComment_header
	$headerCode='';
	if(function_exists('ReportComment_header')){
		$args=array();
		$headerCode=ReportComment_header($x->ContentType, getMemberInfo(), $args);
	}  
	if(!$headerCode){
		include_once("$currDir/header.php"); 
	}else{
		ob_start(); include_once("$currDir/header.php"); $dHeader=ob_get_contents(); ob_end_clean();
		echo str_replace('<%%HEADER%%>', $dHeader, $headerCode);
	}

	echo $x->HTML;
	// hook: ReportComment_footer
	$footerCode='';
	if(function_exists('ReportComment_footer')){
		$args=array();
		$footerCode=ReportComment_footer($x->ContentType, getMemberInfo(), $args);
	}  
	if(!$footerCode){
		include_once("$currDir/footer.php"); 
	}else{
		ob_start(); include_once("$currDir/footer.php"); $dFooter=ob_get_contents(); ob_end_clean();
		echo str_replace('<%%FOOTER%%>', $dFooter, $footerCode);
	}
?>